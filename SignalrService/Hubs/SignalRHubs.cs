using Microsoft.AspNetCore.SignalR;
using System.Threading.Tasks;

namespace SignalrService.Hubs
{

    public class MessageHub : Hub
    {
        //public async Task SendMessage(string title, string name, string message)
        //{
        //    await Clients.All.ReceiveMessage(title, name, message);
        //}
    }

}
